# Paper

This folder is reserved for the LaTeX source of the paper associated with
this repository.

## Overleaf

The paper is currently maintained on Overleaf. Add the project link below:

- Overleaf project: _TBD — paste the share/read-only URL here_

If/when the Overleaf project is synced into this repo (e.g. via the Overleaf
Git integration or as a `git submodule`), the LaTeX sources, figures, and
`main.tex` will live directly under `paper/`.

## Suggested layout (once populated)

```
paper/
├── main.tex
├── sections/
├── figures/
├── bib/
│   └── references.bib
└── README.md
```
